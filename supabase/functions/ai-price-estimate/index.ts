import { serve } from "https://deno.land/std@0.177.0/http/server.ts";

const OPENROUTER_API_KEY = Deno.env.get("OPENROUTER_API_KEY");
const OPENROUTER_MODEL = Deno.env.get("OPENROUTER_MODEL") || "openai/gpt-4o-mini";

const corsHeaders = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
    'Access-Control-Allow-Methods': 'POST, OPTIONS',
};

serve(async (req) => {
    if (req.method === 'OPTIONS') {
        return new Response('ok', { headers: corsHeaders });
    }

    try {
        // Проверяем наличие API ключа
        if (!OPENROUTER_API_KEY) {
            console.error("Missing OPENROUTER_API_KEY");
            return new Response(
                JSON.stringify({
                    aiMin: null,
                    aiMax: null,
                    aiFailed: true,
                    reasonShort: 'API ключ не настроен',
                    reasonLong: 'Сервер не смог подключиться к ИИ. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: []
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        // Безопасное чтение входящего запроса
        let data;
        let rawBody = '';
        try {
            rawBody = await req.text();
            console.log('Raw request body (first 1000 chars):', rawBody.slice(0, 1000));
            data = JSON.parse(rawBody);
        } catch (err) {
            console.error('Failed to parse request JSON in ai-price-estimate:', err);
            console.error('Raw body (first 1000 chars):', rawBody.slice(0, 1000));
            return new Response(
                JSON.stringify({
                    aiMin: null,
                    aiMax: null,
                    aiFailed: true,
                    reasonShort: 'Ошибка разбора запроса',
                    reasonLong: 'Сервер не смог прочитать данные расчёта. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: []
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        // Формируем промт для AI с жёсткими правилами
        const prompt = `Ты опытный мастер-сварщик с 20-летним стажем работы в России. 
Проанализируй заявку на сварочные работы и дай реалистичную оценку стоимости в рублях (₽).

═══════════════════════════════════════════════
📋 ДАННЫЕ ЗАЯВКИ
═══════════════════════════════════════════════

**СТРУКТУРИРОВАННЫЕ ПАРАМЕТРЫ (из формы):**
- Тип работ: ${data.typeOfWork || "не указан"}
- Режим работы: ${data.workScope ?
                (data.workScope === 'pre_cut' ? 'Сварка из заготовок (детали уже нарезаны, только сборка и сварка)' :
                    data.workScope === 'from_scratch' ? 'Изготовление с нуля (разметка, резка, подготовка кромок, подгонка, сборка, сварка)' :
                        data.workScope === 'rework' ? 'Переделка/ремонт (разборка, удаление старых швов, доработка, сварка)' :
                            data.workScope) : "не указан"}
- Материал: ${data.material || "не указан"}
- Толщина: ${data.thickness || "не указана"}
- Тип шва: ${data.seamType || "не указан"}
- Длина швов: ${data.volume || "не указана"}
- Положение: ${data.position || "не указано"}
- Условия работы: ${data.conditions?.join(", ") || "обычные"}
- Материал: ${data.materialOwner === "client" ? "заказчика" : "исполнителя"}
- Срок выполнения: ${data.deadline || "обычный"}
- Дополнительные услуги: ${data.extraServices?.join(", ") || "нет"}

**БАЗОВЫЙ ДИАПАЗОН ЛОКАЛЬНОГО КАЛЬКУЛЯТОРА:**
- Минимум: ${data.localMin || 0} ₽
- Максимум: ${data.localMax || 0} ₽

**ТЕКСТОВЫЕ ПОЛЯ КЛИЕНТА:**
- Описание (Шаг 1): ${data.description || "нет"}
- Уточнения по материалам (Шаг 2): ${data.descriptionStep2 || "нет"}
- Комментарий по условиям (Шаг 3): ${data.descriptionStep3 || "нет"}

**СЛУЖЕБНЫЕ ДАННЫЕ:**
- Количество фото: ${data.photos?.length || 0} шт.

═══════════════════════════════════════════════
🚫 СТРОГИЕ ПРАВИЛА (ОБЯЗАТЕЛЬНЫ ДЛЯ ВЫПОЛНЕНИЯ)
═══════════════════════════════════════════════

1. **ЗАПРЕЩЕНО ВЫДУМЫВАТЬ ДАННЫЕ**
   ❌ НЕ придумывай размеры, длину швов, толщину, материал, тип шва
   ✅ Используй ТОЛЬКО те числа и параметры, которые явно переданы из формы или из текстовых полей
   ✅ Если данные неоднозначны или отсутствуют — укажи это в warnings

2. **ИСПОЛЬЗОВАНИЕ ТЕКСТОВЫХ ПОЛЕЙ**
   - Текстовые поля (descriptionStep2, descriptionStep3) используй для:
     * понимания типа конструкции
     * оценки сложности доступа
     * оценки рисков (высота, стеснённый доступ, опасные условия)
   - Если в тексте явно указаны размеры/толщина/материал:
     * Если соответствующее поле формы пустое или "Не знаю" — можешь использовать данные из текста
     * Если поле формы заполнено — приоритет у формы, текст только для подтверждения
   - Примеры усложняющих факторов из текста:
     * "на высоте", "над дорогой", "над водой", "в колодце"
     * "тесно подлезть", "нужно ночью", "горячий трубопровод"
     * "жёсткие требования НК", "опасная зона", "действующие коммуникации"

3. **ФОТОГРАФИИ**
   - Используй фото ТОЛЬКО для:
     * оценки сложности доступа
     * определения типа конструкции
     * проверки, что это действительно сварка
   - ❌ НЕ используй фото для определения размеров или толщины "на глаз"

4. **РЕЖИМ РАБОТЫ (workScope) — КРИТИЧЕСКИ ВАЖНО ДЛЯ ОЦЕНКИ**
   Режим работы напрямую влияет на трудозатраты и уже УЧТЁН в localMin/localMax.
   Локальный калькулятор применяет следующие коэффициенты:
   
   📦 **pre_cut (Сварка из заготовок)** — БАЗОВЫЙ УРОВЕНЬ (1.0x)
      - Детали уже нарезаны, кромки подготовлены
      - Нужна только сборка и сварка
      - Минимум подготовительных работ
      - Это самый дешёвый вариант
   
   🏗️ **from_scratch (Изготовление с нуля)** — ДОРОЖЕ на 40-80%
      - Разметка заготовок по чертежам
      - Резка металла (болгаркой, плазмой, гильотиной)
      - Механическая обработка кромок под сварку
      - Подгонка деталей (исправление резки, подпиливание)
      - Сборка в приспособлениях или прихватками
      - Сварка
      - Коэффициенты: weld=1.15x, prep=1.8x (резка+подгонка), finish=1.2x
   
   🔧 **rework (Переделка/ремонт)** — САМЫЙ ДОРОГОЙ (+60-100%)
      - Разборка/демонтаж существующей конструкции
      - Удаление старых сварных швов (зачистка, вырубка, срезка)
      - Зачистка загрязнённого металла, ржавчины, краски
      - Сложный доступ к местам сварки (неудобное положение)
      - Подгонка старых деталей (деформация, коррозия)
      - Сварка в неудобных местах, часто по старому металлу
      - Дополнительные риски: скрытые дефекты, трещины, непроваренные участки
      - Коэффициенты: weld=1.25x, prep=2.0x (разборка+зачистка), finish=1.4x
   
   **ТВОЯ ЗАДАЧА:**
   - Базовая цена localMin/localMax УЖЕ включает workScope
   - НЕ увеличивай цену повторно за режим работы
   - Но ОБЯЗАТЕЛЬНО объясни в reason_long, почему данный режим дороже/дешевле
   - Для from_scratch и rework упомяни дополнительные работы (резка, разборка, зачистка)
   - Для rework добавь в warnings риски: скрытые дефекты, непредсказуемое состояние металла

5. **КОРРЕКТИРОВКА БАЗОВОГО ДИАПАЗОНА ЦЕНЫ**
   Локальный калькулятор даёт базовый диапазон localMin–localMax для обычных условий.
   
   **Правила корректировки:**
   - В типовых условиях: можешь скорректировать ±30% от localMin/localMax
   - При явных усложняющих факторах: до +50% максимум
     (высота, ночные работы, стеснённый доступ, срочно, жёсткие требования НК, опасные условия)
   - ❌ НЕЛЬЗЯ уменьшать цену ниже localMin
   - ❌ НЕЛЬЗЯ увеличивать более чем в 1.5 раза без крайне веских причин

6. **ФОРМАТ ОТВЕТА**
   Верни СТРОГО JSON без дополнительного текста:
   {
     "price_range": {
       "min": number,
       "max": number
     },
     "reason_short": "Короткое объяснение для блока рядом с ценой (1-2 предложения)",
     "reason_long": "Развёрнутое коммерческое предложение от лица опытного сварщика",
     "warnings": ["Список предупреждений, если данные сомнительные или нужен выезд"]
   }

═══════════════════════════════════════════════
📝 ТРЕБОВАНИЯ К СОДЕРЖАНИЮ ОТВЕТА
═══════════════════════════════════════════════

**reason_short:**
- 1-2 предложения
- Коротко, понятным языком
- Без пафоса и маркетингового бреда
- Объясни ключевые факторы цены

**reason_long (готовое коммерческое предложение для копирования клиенту):**
- Объём: 600–1200 символов
- БЕЗ эмодзи, БЕЗ обращения по имени, ТОЛЬКО факты и спокойная аргументация
- Структура КП СТРОГО в следующем порядке:

1. ВСТУПЛЕНИЕ (1-2 предложен

ия):
   «Здравствуйте! Отправляю ориентировочный расчёт по вашему заказу по сварочным работам.»

2. ИСХОДНЫЕ ДАННЫЕ (2-3 предложения):
   Кратко опиши конструкцию и объём работ из полей: description, typeOfWork, material, seamType, volume, workScope.
   Формат: «Исходные данные: [описание]. Материал – [material], тип шва – [seamType], объём работ – [volume или краткое из description].»

3. СТОИМОСТЬ (2-3 предложения):
   «Ориентировочная стоимость работ: от [price_range.min] до [price_range.max] ₽.»
   Объясни разброс: зависит от точных размеров, доступа к месту работ, необходимости дополнительных операций.

4. ЧТО ВХОДИТ В РАБОТЫ (список, 4-6 пунктов):
   Адаптируй под typeOfWork и workScope:
   - Разметка и подготовка по чертежу/месту
   - Резка заготовок и подготовка кромок (если workScope = from_scratch или rework)
   - Сборка конструкции в размер
   - Сварка с соблюдением технологии
   - Контроль качества швов, зачистка по необходимости
   - (Если подходит) Испытания/опрессовка/проверка герметичности

5. ЗАЩИТА ОТ ДЕШЁВЫХ ПОДРЯДЧИКОВ (3-4 предложения):
   Объясни, почему цена именно такая:
   - Это работа с нуля/ремонт (в зависимости от workScope), а не просто «схватить шов»
   - Риски дешёвой работы: деформация, протечки, переделка, простой, нарушение требований безопасности
   - Выгода клиента: «делаем один раз и надолго», соблюдение ГОСТ/технических требований (БЕЗ указания конкретных номеров ГОСТов)

6. УСЛОВИЯ (1-2 предложения):
   «Расчёт предварительный. Окончательная цена фиксируется после выезда на объект / получения детальных чертежей / фото.
   Сроки и точную стоимость уточним после согласования всех деталей.»

7. ПРИЗЫВ К ДЕЙСТВИЮ (1 предложение):
   «Если вас устраивает указанный диапазон цен, можем перейти к согласованию деталей и подготовке точного коммерческого предложения.»

**warnings:**
- Используй, если:
  * данные исходные неточные или противоречивые
  * нужен выезд для точной оценки
  * есть технические риски
  * требуется дополнительная информация
- Если всё ясно — оставь пустым массивом []

═══════════════════════════════════════════════

Проанализируй заявку и верни JSON согласно правилам выше.`;

        // Вызываем OpenRouter API с обработкой таймаута
        const controller = new AbortController();
        const timeoutId = setTimeout(() => controller.abort(), 15000); // таймаут 15 секунд

        let response;
        try {
            response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
                method: "POST",
                headers: {
                    "Authorization": `Bearer ${OPENROUTER_API_KEY}`,
                    "Content-Type": "application/json",
                    "HTTP-Referer": "https://argo-weld-calc.com",
                    "X-Title": "ARGO Weld Calculator"
                },
                body: JSON.stringify({
                    model: OPENROUTER_MODEL,
                    messages: [
                        {
                            role: "user",
                            content: prompt
                        }
                    ],
                    temperature: 0.3,
                    max_tokens: 2500 // увеличено для полноценного КП (600-1200 символов)
                }),
                signal: controller.signal
            });
        } catch (fetchError) {
            clearTimeout(timeoutId);
            console.error("OpenRouter fetch error:", fetchError);
            return new Response(
                JSON.stringify({
                    aiMin: null,
                    aiMax: null,
                    aiFailed: true,
                    reasonShort: 'Ошибка подключения к ИИ',
                    reasonLong: 'Сервер не смог подключиться к ИИ. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: []
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        clearTimeout(timeoutId);

        if (!response.ok) {
            const errorData = await response.text();
            console.error("OpenRouter API error:", errorData);
            return new Response(
                JSON.stringify({
                    aiMin: null,
                    aiMax: null,
                    aiFailed: true,
                    reasonShort: 'Ошибка API ИИ',
                    reasonLong: 'Сервер не смог получить ответ от ИИ. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: []
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        // Безопасное чтение ответа от OpenRouter
        const raw = await response.text();
        console.log('OpenRouter raw response (first 1000 chars):', raw.slice(0, 1000));

        let aiResponse: any;
        try {
            aiResponse = JSON.parse(raw);
        } catch (err) {
            console.error('Failed to parse OpenRouter JSON in ai-price-estimate:', err);
            console.error('Raw response:', raw);
            return new Response(
                JSON.stringify({
                    aiFailed: true,
                    reasonShort: 'Ошибка разбора ответа ИИ',
                    reasonLong: 'Сервер не смог прочитать данные расчёта. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: ['Служебное сообщение: не удалось разобрать JSON в Edge-функции.'],
                    aiMin: null,
                    aiMax: null,
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        const content = aiResponse.choices?.[0]?.message?.content;

        if (!content) {
            console.error("No content in AI response");
            return new Response(
                JSON.stringify({
                    aiMin: null,
                    aiMax: null,
                    aiFailed: true,
                    reasonShort: 'Пустой ответ от ИИ',
                    reasonLong: 'ИИ вернул пустой ответ. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: []
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        // Парсим JSON из ответа AI
        let aiResult;
        try {
            // Пытаемся извлечь JSON из ответа (на случай если AI добавил текст до/после)
            const jsonMatch = content.match(/\{[\s\S]*\}/);
            if (jsonMatch) {
                aiResult = JSON.parse(jsonMatch[0]);
            } else {
                aiResult = JSON.parse(content);
            }
        } catch (parseError) {
            console.error("Failed to parse AI response:", content);
            return new Response(
                JSON.stringify({
                    aiMin: null,
                    aiMax: null,
                    aiFailed: true,
                    reasonShort: 'Ошибка парсинга ответа ИИ',
                    reasonLong: 'ИИ вернул некорректный формат данных. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: []
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        // Валидируем структуру ответа
        if (
            !aiResult.price_range ||
            typeof aiResult.price_range.min !== "number" ||
            typeof aiResult.price_range.max !== "number" ||
            aiResult.price_range.min <= 0 ||
            aiResult.price_range.max <= 0 ||
            aiResult.price_range.min > aiResult.price_range.max ||
            typeof aiResult.reason_short !== "string" ||
            typeof aiResult.reason_long !== "string"
        ) {
            console.error("Invalid AI result structure:", aiResult);
            return new Response(
                JSON.stringify({
                    aiMin: null,
                    aiMax: null,
                    aiFailed: true,
                    reasonShort: 'Некорректная структура ответа ИИ',
                    reasonLong: 'ИИ вернул данные в неожиданном формате. Показана базовая стоимость по внутреннему калькулятору.',
                    warnings: []
                }),
                { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
            );
        }

        // Возвращаем успешный результат от AI
        return new Response(
            JSON.stringify({
                aiMin: Math.round(aiResult.price_range.min),
                aiMax: Math.round(aiResult.price_range.max),
                reasonShort: aiResult.reason_short,
                reasonLong: aiResult.reason_long,
                warnings: aiResult.warnings || [],
                aiFailed: false
            }),
            { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
        );

    } catch (err) {
        console.error("Error in ai-price-estimate:", err);
        return new Response(
            JSON.stringify({
                aiMin: null,
                aiMax: null,
                aiFailed: true,
                reasonShort: 'Внутренняя ошибка сервера',
                reasonLong: 'Произошла непредвиденная ошибка. Показана базовая стоимость по внутреннему калькулятору.',
                warnings: []
            }),
            { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
        );
    }
});
