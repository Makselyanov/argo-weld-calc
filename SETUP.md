# ARGO-72 | Калькулятор сварочных работ

Web-приложение для расчёта стоимости сварочных работ с glassmorphism-дизайном.

## Технологический стек

- **Frontend**: React + TypeScript + Vite
- **Стилизация**: Tailwind CSS (glassmorphism-стиль)
- **Роутинг**: React Router
- **Хранение данных**: LocalStorage (пока без бэкенда)

## Установка и запуск

### Требования
- Node.js 18+ и npm (рекомендуется устанавливать через [nvm](https://github.com/nvm-sh/nvm))

### Локальная разработка

1. **Клонируйте репозиторий**
```bash
git clone <YOUR_GIT_URL>
cd <PROJECT_NAME>
```

2. **Установите зависимости**
```bash
npm install
```

3. **Запустите dev-сервер**
```bash
npm run dev
```

Приложение откроется на `http://localhost:8080`

### Доступные npm-скрипты

- `npm run dev` - запуск dev-сервера с hot reload
- `npm run build` - production-сборка
- `npm run preview` - предпросмотр production-сборки
- `npm run lint` - проверка кода

## Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── GlassCard.tsx   # Компонент glass-эффекта
│   ├── GlassButton.tsx # Кнопка с glass-эффектом
│   └── ParameterChip.tsx # Чип для выбора параметров
├── pages/              # Страницы приложения
│   ├── Home.tsx        # Главная страница
│   ├── NewCalculation.tsx # Мастер создания расчёта
│   ├── History.tsx     # История расчётов
│   ├── CalculationDetail.tsx # Детали расчёта
│   ├── OrderConfirmation.tsx # Подтверждение заказа
│   └── ForWelders.tsx  # Страница для сварщиков (placeholder)
├── services/           # Бизнес-логика
│   └── calculationService.ts # Работа с расчётами
├── types/              # TypeScript типы
│   └── calculation.ts  # Интерфейсы расчётов
└── index.css          # Дизайн-система (glassmorphism)
```

## Основные возможности

### Текущий функционал (v1.0 - Frontend)

✅ **Главная страница** - три основных кнопки навигации
✅ **Мастер расчёта** - пошаговый процесс:
  - Шаг 1: Загрузка фото и описание задачи
  - Шаг 2: Выбор параметров работ (тип, материал, толщина и т.д.)
  - Шаг 3: Оценка стоимости с доп. услугами
✅ **История расчётов** - список всех созданных расчётов
✅ **Детали расчёта** - полная информация по выбранному расчёту
✅ **LocalStorage** - автосохранение всех расчётов
✅ **Glassmorphism UI** - современный дизайн с эффектом матового стекла

### Будущие интеграции (готово к подключению)

🔜 **Supabase Backend**:
  - Аутентификация пользователей
  - Хранение расчётов в PostgreSQL
  - Edge Functions для обработки

🔜 **OpenRouter API**:
  - AI-оценка стоимости по фото
  - Умные рекомендации

🔜 **Telegram Mini App**:
  - Интеграция Telegram WebApp SDK
  - Telegram Stars для оплаты
  - Авто-авторизация через Telegram

## Дизайн-система

Все стили определены в `src/index.css` через CSS-переменные:

- **Цвета**: dark gradient фон, cyan/turquoise акценты, orange для CTA
- **Glassmorphism**: backdrop-blur, полупрозрачность, мягкие тени
- **Анимации**: плавные transitions и hover-эффекты

**Важно**: Используйте semantic tokens из дизайн-системы, не хардкодьте цвета!

## Логика расчёта цен

Текущая версия использует mock-формулу в `calculateBasePrice()`:

```typescript
Базовая цена × Тип работ × Материал × Толщина × 
Положение × Условия × Материал исполнителя × Срок
```

Коэффициенты настроены в `src/services/calculationService.ts`

## Подготовка к production

1. Создайте production build:
```bash
npm run build
```

2. Файлы соберутся в папку `dist/`

3. Разверните на хостинге:
   - Lovable Cloud (один клик)
   - Vercel, Netlify, GitHub Pages
   - Любой статический хостинг

## Roadmap

**Phase 1 (Текущая)**: ✅ Frontend-only версия с localStorage
**Phase 2**: Backend (Supabase) + база данных
**Phase 3**: AI-оценка через OpenRouter
**Phase 4**: Telegram Mini App интеграция
**Phase 5**: Telegram Stars payments

## Поддержка

При вопросах или проблемах создавайте issue в репозитории проекта.
